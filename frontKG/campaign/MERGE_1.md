# MERGE_1 — frontKG 第一幕合并与第二幕作战令（2026-09-13，编排者）

第一幕 11 lane 完毕（lane_01、03、09、09b、09c、11–15 中 11 个完成、c1b、dm）。本文件是第二幕 lane 的公共依赖，只读。

## 一、战役资产盘点

**基础设施（全部可复用）**：verifier.py（selftest 20/20，精确算术）+ 图库（n=5..8，n=6 全 76 骨架完备）+ SAT/DRAT 管线（kissat+drat-trim，双实现对照纪律）+ final_judge/iso5_branch/kge4 分支求解器 + 4-connected 定义链文档（definitions.md，全战役权威）。

**(n=6, d=3) 排除进度（€3000 靶）**：
- iso4：≤35 全基数闭合（lane_12 ≤30 + lane_14 ≤35，DRAT 认证）；36..40 穷尽归结为 **51 个 PENDING 对象**；S₉₀（90 实体双自由相容支撑）否证了 (†)/(‡) 任意基数靶——第三构形存在但未找到。
- iso5：≤28 全基数闭合（lane_13，DRAT×4）；塌缩机制 K≤70 域 100% C4 型，≥85 机制换挡（模式 A 失效）。
- κ≥4 类：k\*∈[21,24] 层已有部分证书，≥k\* 未动（lane_15 被中断，批10 重启）。
- **剩余坐标：iso4 ≥36（36..40 精确到 51 对象）∪ iso5 ≥29 ∪ 9 个 κ≥4 类 ≥k\***。零标红候选（截至批9）。

**证明翼**：极小反例结构定理 T-A（修正 CGI24 推论缺口 GAP-2）、LI 消去定理 T-B、定理 D（Δ≤4 ⟹ 双端碰撞交换系统）、塌缩代数引理 12.B/14.B（模式 A/B 双双升格定理）。撞车情报：DeepMind DMFC26=CGI24 推论（零覆盖增量），时间窗约 6 个月，反例必含双色边、d=3。

**公开修正记录**：lane_09b→09c 交接丢数据入档；lane_11 两处险情（verdict 覆盖、superset-blocking）；lane_12 独立验证器 2 bug；lane_14 3 bug（极性反转/变元碰撞/元组排序）——**全部被交叉对照与证书回放抓获，零事故流出。**

## 二、第二幕作战地图

```
(6,3) 完备排除 = iso4 ≥36 ∪ iso5 ≥29 ∪ κ≥4 ≥k\* 三支全灭
  ├─ iso4 36..40：51 个 PENDING 对象 → 第 4 层精确符号求解（lane_16b）
  ├─ iso5 ≥29：塌缩机制 + 机制换挡应对（"高基数可约减"引理是双用钥匙）
  ├─ κ≥4 ≥k\*：lane_15b 继承重启
  └─ 证明翼并行：高基数可约减引理 / 第三构形 (⋯) 的存在性分类
反向（证明翼）：若 51 个对象中出现权值可行者 = 反例候选（标红协议），€3000 流程启动
```

**第二幕主靶（优先级序）**：
1. **lane_15b**：κ≥4 ≥k\* 分支（唯一未动的剩余支）。
2. **lane_16b**：51+62 个 PENDING 对象的第 4 层精确符号求解——这是"反例是否存在"的直接判决，任何可行=标红。
3. "高基数可约减"引理（lane_13/14 双双指向的通用钥匙）。
4. 时间窗管理：批12 后若 (6,3) 未闭合，评估"部分排除结果"的发布策略（对照 DeepMind Q4 in-prep 论文）。

**明确死路（勿再投）**：随机染色采样（lane_c1b 判死）；(†) 任意基数版（S₉₀ 否证）；模式 A 在 ≥85 区的直推。

## 三、第二幕纪律增量

1. PENDING 对象求解必须四层全过（组合必要条件→SAT 相容→权值方程→精确符号复验）才可写"可行"；任何候选执行标红协议（双参数+独立进程+编排者复核）。
2. 实现 bug 零容忍披露：每 lane 报告 §"过程纪律"节必须列全部抓获的 bug（沿用批8/9 惯例，这是对外可信度的证据链）。
3. 每批后编排者核对剩余坐标表并更新本文件。
