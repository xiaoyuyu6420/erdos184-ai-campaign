# AI 数学研究流水线 · 方法论档案

> 2026-09-08 至 09-12 实战沉淀。从「Erdős #213 整距离点集」单点开工，发展到多题并行、
> 64 路子代理战役、完整红蓝审查链与发布流水线。本文是这套打法的操作手册。

## 一、核心思想

**题海 + 分工 + 对抗**。不押单一难题：题库（erdosproblems.com 等）里题目管够，
流水线只管挖——出什么收什么。算力只用于两处：子代理推理（API，主要算力）与
最终机器验证（本地 CPU，次要）。**选题主攻"LLM 比较优势区"**：证明型、工具箱
文献化、验证便宜、搜索有界；纯穷举型题目只在机器够得着时开。

## 二、角色分工（子代理编制）

| 角色 | 职责 | 纪律要点 |
|---|---|---|
| 侦察员 scout | 题库全量扫描，按"证明型/计算型、拥挤度、新颖性"筛靶 | 每题给精确陈述+攻击面+撞车风险 |
| 文献员 librarian | 查新：问题页、论坛动态、arXiv、OEIS、作者 repo | 逐条给来源 URL；标注置信度 |
| 攻击手 prover | 每路一个独立角度，禁读其他路产出 | 产出三选一：完整证明/严格部分结果/障碍分析；推不过去标 GAP；不编造引用 |
| 审查员 verifier | 对抗性复核，**默认有错**，找不到错才 PASS | 独立重写验证脚本（不 import 上游代码）；跨报告矛盾做仲裁 |
| 蓝队 blue-team | 按判决书做有界修复，单次通过 | 根因级修复不遮症状；处置记录带诚实信心标注 |
| 写手/校对 | 发布稿创作与独立校对 | 诚实声明独立成章；校对立场=挑错优先 |
| 编排者 orchestrator | 选题拍板、批次调度、情报合并、对外动作审批 | 每幕之间做合并；动态调配火力 |

## 三、战役编排（64 路标准结构）

**三幕**：第一幕·多样性（21 路，每路独立角度，禁互读）；第二幕·深化（24 路，
按第一幕合并后的"差一步"线索围攻）；第三幕·对抗收拢（19 路，红队攻击一切声称
+ 幸存者整理）。合并备忘（MERGE_*.md）在每幕之间沉淀情报、修正地图（示例：
Lane12 曾证明 Lane01 的相变解读方向反了，火力随之重分配）。

**并发纪律**：3 路 API 并发为限（实测 4 路触发账户限流）；撞限流撤一路；
5 小时窗口耗尽则波浪推进。**遗稿继承**：中断的 lane 留下的草稿/脚本由下一阵
继承续写，不推翻重来——零损失换人。

### 3.5 OpenAI CDC prompt 增补（2026-09-12）

OpenAI 2026-07 用 GPT-5.6 Sol Ultra + 64 subagent 约 1 小时证明循环双覆盖猜想
（CDC，开放 50+ 年），并开源了 orchestrator prompt 全文（存档
`openai-cdc/cdc_prompt.pdf`，三页证明 `cdc_proof.pdf`，Lean 形式化
github.com/openai/cdc-lean）。其设计与本手册三幕结构高度同构，以下六点为
经它验证有效、即日起并入标准协议的增补：

1. **Lane prompt 零歧义条款**：开跑前固定全部术语定义与边界情形（空图/非连通/
   平行边/重数含义），枚举"什么算、什么不算"（特例、有限规模计算验证、归约到
   另一未证猜想、无完备证书的候选反例，均不算解决主问题——特例类定理按战役
   目标另定分级）。
2. **正面先验 + 封死逃跑出口**："假定存在完整肯定证明"；不许回答"问题
   仍开放"交差；单 lane 至少试 3 条子路线再报死（替代其 8 小时下限——我们
   lane 生命短，用批间续投补）。
3. **方法族登记表（root 职责）**：每批结束把 lane 按**数学思想**（非措辞）
   归族入表，状态机 active / blocked / dead / won；停在"与原问题等强度引理"
   处的族标 blocked，无新机制不重开；某族过热即向冷门视角分流。
4. **交叉授粉时机**：不相容路线各自推进到暴露真实强弱之后才允许互通——
   第一幕严格执行禁互读，第二幕合并才有资格谈"融合路线"。
5. **对抗清单按题定制**：CDC 版（精确两次乘数、伪装圈的重复边闭迹、平行边
   2-圈、非连通、割点、归约引入的桥、循环论证）是范本；每场战役开打前由
   root 写出本题专属版，列入每 lane 的自检义务。
6. **拒绝空话条款**：lane 回报必须是"具体引理/构造/方程/反例"；状态报告、
   模糊乐观、"routine 细节省略"一律打回。

**两点刻意差异**（不照抄 OpenAI）：①我们保留查新义务（librarian 阶段 +
每 3-5 批撞车监控）——它是后来者，"不知别人已解决"比"被提示污染"更贵；
②计算红线明文化：本机 16GB Mac，单任务 ≤小时级 CPU / 数 GB 内存，超了缩
规模不硬扛（OpenAI 未披露其算力边界）。

CDC 结果侧写（启示）：胜出路线非新发明，而是已知部件的新组合（cubic 归约
+ F₂³ nowhere-zero 流 + 每边二元标签集 + F₂ 线性系统相容性 + 对偶论证）。
64 路的价值不在算力，在并行试错空间里保住"冷门组合"的存活机会。

## 四、验证金字塔（对抗"AI 自信伪证明"）

从便宜到贵，逐层收窄错误率（实战累计抓出 **12 处错误**，含 2 处"标称完全证明
实则有洞"、1 处方向性误读、1 处陈述本身为假）：

1. **攻击手自检**（自我纠错留痕，如 witness 复算、oracle 双实现）
2. **独立审查员**（不共享代码路径的复算脚本；固定随机种子可复现）
3. **机器穷举**（小图全量、参数组合全枚举——最不会骗人的证据）
4. **跨 lane 交叉确认**（同一结论被两条独立路线同时抵达才标"双路确认"）
5. **外部 AI 评审**（非同源模型，抓 folklore 盲区与传播风险——内部 lane 级
   检索查得出正式先例，查不出"专家懒得写的显然事实"，需要外部视角）
6. **人类专家验证**（给原论文作者发信"恳请验证"；最终负责人永远是人）

## 五、事实边界与诚实条款

- 只声称自己算过/证过的；排他结论必须声明所用素数域/直径范围/前提；
- 负结果（反例、障碍地图、"此路不通"证明）**与正结果同等入档**；
- 撤回留痕：被推翻的声称不从文件里删掉，而是标注撤回原因与替代结论
  （实战：相变命题方向读反、命题 12.3(b) 被星形反例击毙，均留档）；
- novelty 盲区声明：文献检索查不出 folklore 状态，"部分结果可能属于已知"
  必须写进发布稿显著位置；
- 发布分层与风险匹配：知乎/GitHub（低风险，诚实框架兜底）→ 原作者邮件
  （措辞="恳请验证"）→ arXiv/期刊（挂起，直到专家认可或 Lean 形式化）。

## 六、本地算力运维（16GB Mac 的生存法则）

- 单题一个目录（lab/front213、front203、front184、front20…），互不越界；
- 长跑程序必带：checkpoint 分块续跑（chunk 细到 50 单位）、内存守卫
  （RSS+swap 双绊线，击杀后自愈降档续跑，单飞锁防重复拉起）；
- **共享基础设施纪律（2026-09-14 事故教训）**：子代理不得修改守卫、链条
  运行器等共享脚本——曾有代理出于好意把守卫回写成老版本并重启，抹掉
  自愈逻辑，导致与其任务共存的另一条战线被反复击杀 6 小时。发现问题
  写进报告移交编排者，不得自行重写；
- **阈值设计原则**：守卫阈值必须落在资源运行带之外——实测 swap 空闲基线
  ~11GB、与大任务共存峰值 ~14GB，阈值设 12.3/13.3GB 都在带内，必然
  「杀-自愈-再杀」振荡；正确做法是把 kill 线抬到带外（15.3GB）；
- **macOS 坑**：无 `flock` 命令（Linux util-linux 工具）——单飞锁用
  mkdir 原子锁 + pid 存活检查实现；接管逻辑必须覆盖「锁路径存在但是
  文件/无 pid/进程已死」三种陈旧情形；
- 磁盘闸：chunk 启动前检查剩余空间，宁停不炸；可再生物（大 CNF 等）
  删除前留再生文档；
- GPU/NPU 不适用分支密集整数搜索与 SAT——不硬上；
- 线程数按内存水位自适应：swap 高→降档，低→升档，全自动；
- 防御要分层：cstage45 的「被杀秒级重试（3 次）」是第一层，memwatch 的
  「整链死亡检测 → 5 分钟自愈」是兜底层——单层防御扛不住持续击杀。

## 七、战果档案（2026-09-08 ~ 09-12）

- **#184**：15+ 定理级结果（ce(K_{2m})=2m−1 双路确认、块可加性恒等式、
  Eulerian 环面图 ≤n−1、Dirac 正则 ≤n−1、d=3 常数 4/3 等）、4 个反例/否定、
  BM 证明解剖地图（log* 锁死链）；已发布 GitHub + 知乎
- **AABC25 六正则猜想**：解决并强化（ce≤n−2），独立审查有条件放行，
  待原作者确认
- **#213**：排他数据推进（两已知七点 10⁶ 延伸排除），30k/70k 扫描进行中
- **#20**：名题彩票开局（Rao 解剖 + k=3 主攻）
- **#203**：基础设施 + K13/G14 双认证 + 硬件门槛确认（需 ≥32GB 机器）
